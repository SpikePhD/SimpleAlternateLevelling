#pragma once

namespace EA::InfoPages {
    // Registers the Stats and XP Log pages under "Simple Alternate Levelling"
    // in SKSE Menu Framework. Call after SettingsPage::Register succeeded.
    void Register();
}
